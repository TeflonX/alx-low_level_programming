#include "main.h"
/**
 * _abs - prints the absolute vvalue of a number
 *
 * Return: n and -n.
 *
 * @n: integer
 */
int _abs(int n)
{

	if (n <= 0)
	{
		n = (-1) * n;
		return (-n);
	}
	else
	{
		n;
		return (n);
	}
}
