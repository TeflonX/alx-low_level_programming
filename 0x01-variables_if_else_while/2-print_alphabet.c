#include <stdio.h>
/**
 * main - prints the alphabet in lower case.
 *
 * Return: 0 (Success)
 */
int main(void)
{
	char i;

	i = getchar;

	for (i = 'a'; i <= 'z'; i++)
	{
		putchar(i);
		putchar('\n');
	}
	return (0);
}
