#include <stdio.h>
#include <stdarg.h>
#include <stdlib.h>
#include <string.h>

void print_all(const char * const format, ...)
{
	int i, len;

	va_list args;

	len = strlen(format);
	va_start(args, format);






}
