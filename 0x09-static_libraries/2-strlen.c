#include "main.h"

/**
 * _strlen - calculate the length of a string.
 * Return: an integer, i.
 * @s: pointer to a char data type.
 */

int _strlen(char *s)
{
	int i;

	for (i = 0; s[i] != '\0'; i++)
	{
	}

	return (i);
}
