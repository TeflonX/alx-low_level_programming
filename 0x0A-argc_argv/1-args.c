#include <stdio.h>
#include <stdlib.h>
/**
 * main - prints the number of arguent passed.
 * Return: 0 (Success)
 * @argc: argument count.
 * @argv: argument vector.
 */
int main(int argc, char *argv[])
{
	(void)argv;
	printf("%d\n", argc);

	return (0);
}
